<%= form_for(@bike_model) do |f| %>
  <% if @bike_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bike_model.errors.count, "error") %> prohibited this bike_model from being saved:</h2>

      <ul>
      <% @bike_model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bike_model_name %><br>
    <%= f.text_field :bike_model_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
